// ============================================================
// Tailwind CSS 設定ファイル
// ここを書き換えるとアプリ全体のデザインが変わります。
// ============================================================

import type { Config } from "tailwindcss";

const config: Config = {
  // Tailwind CSS が適用されるファイルの場所
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
    "./constants/**/*.{js,ts}",
  ],
  theme: {
    extend: {
      // ============================================================
      // ★ アプリのブランドカラー設定 ★
      //
      // 【重要】ここを変えるだけで、アプリ全体の色が一度に変わります。
      //   例: primary の値を別の色コードに変えてみてください。
      // ============================================================
      colors: {
        // ── メインカラー ──────────────────────────────────────────
        // ボタン・アイコン選択状態・アクセントに使う「主役の色」
        // Deep Turquoise Blue: 「深海の思考を空へ繋ぐ」ストーリーの色
        primary: "#006D77",

        // ── カード背景色 ──────────────────────────────────────────
        // カードや入力欄の背景に使う「ほんのり薄いグレー」
        // 白との微妙な色差がボーダー線なしで立体感を生み出します
        surface: "#F8FAFC",

        // ── テキストカラー ────────────────────────────────────────
        // 本文・見出しに使う「漆黒に近い濃いスレート」
        // わずかに青みを含み、純黒より読みやすい
        ink: "#0F172A",

        // ── 補助テキスト・非アクティブ ────────────────────────────
        // 日付・説明文・非選択アイコンなどの「薄いグレー」
        muted: "#94A3B8",
      },

      // ============================================================
      // ボタン・タップ領域の最小サイズ
      // 指で押しやすいよう 56px 以上を推奨します
      // ============================================================
      minHeight: {
        touch: "56px",
      },
      minWidth: {
        touch: "56px",
      },

      // ============================================================
      // フォント設定（CSS 変数経由で Inter と Noto Sans JP を参照）
      // ============================================================
      fontFamily: {
        sans: ["var(--font-inter)", "var(--font-noto)", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
