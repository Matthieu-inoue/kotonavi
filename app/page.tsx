// ============================================================
// トップページ（/）
// アクセスしたらギャラリー画面に自動でリダイレクトします。
// ============================================================

import { redirect } from "next/navigation";

export default function Home() {
  // トップページ（/）にアクセスしたらギャラリーへ転送します
  // リダイレクト先を変えたい場合は "/gallery" の部分を変えてください
  redirect("/gallery");
}
